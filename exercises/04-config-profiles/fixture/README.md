# Config Fixture

This folder is a fake Codex setup for Exercise 04.

- `fake-home/.codex/config.toml` represents a user's default Codex settings.
- `fake-home/.codex/deep-review.config.toml` represents a named profile for slower review work.
- `sample-project/.codex/config.toml` represents project-local config for one trusted repo.
- `sample-project/AGENTS.md` represents project instructions that should guide Codex behavior.

Do not copy these files into a real Codex home directory. They intentionally contain settings that should be reviewed before use.
