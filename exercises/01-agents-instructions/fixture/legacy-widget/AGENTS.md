# Legacy Widget Agent Notes

## Fixture Scope

- Work only inside this `legacy-widget` fixture unless the exercise brief says otherwise.
- The widget implementation lives in `src/widget.js`.
- Tests live in `tests/widget.test.js`.
- Do not add a build system or rewrite the package structure for small behavior changes.

## Validation

- Run `npm test` from this directory before finishing.
- The test script executes `node tests/widget.test.js`.

## Safety

- Keep edits focused on the requested change and avoid touching source or tests when the task is only documentation or instructions.
